/**
  * Automatically generated file. Please do not edit.
  * @author Highcharts Config Generator by Karasiq
  * @see [[http://api.highcharts.com/highcharts]]
  */
package com.highcharts.native

import scalajs.js, js.`|`
import org.scalajs.dom

import com.highcharts.CleanJsObject
import com.highcharts.HighchartsUtils._

/**
  * @note JavaScript name: <code>navigation-bindings</code>
  */
class NavigationBindings extends com.highcharts.HighchartsGenericObject {

  /**
    * <p>A line with arrow annotation. Includes <code>start</code> and one event in <code>steps</code>
    * array.</p>
    * @since 7.0.0
    */
  var arrowInfinityLine: js.Any = js.undefined

  /**
    * <p>A ray with an arrow annotation bindings. Includes <code>start</code> and one event
    * in <code>steps</code> array.</p>
    * @since 7.0.0
    */
  var arrowRay: js.Any = js.undefined

  /**
    * <p>A segment with an arrow annotation bindings. Includes <code>start</code> and one
    * event in <code>steps</code> array.</p>
    * @since 7.0.0
    */
  var arrowSegment: js.Any = js.undefined

  /**
    * <p>A circle annotation bindings. Includes <code>start</code> and one event in
    * <code>steps</code> array.</p>
    * @since 7.0.0
    */
  var circleAnnotation: js.Any = js.undefined

  /**
    * <p>Crooked line (three points) annotation bindings. Includes <code>start</code> and two
    * events in <code>steps</code> (for second and third points in crooked line) array.</p>
    * @since 7.0.0
    */
  var crooked3: js.Any = js.undefined

  /**
    * <p>Crooked line (five points) annotation bindings. Includes <code>start</code> and four
    * events in <code>steps</code> (for all consequent points in crooked line) array.</p>
    * @since 7.0.0
    */
  var crooked5: js.Any = js.undefined

  /**
    * <p>Hides/shows two price indicators:</p>
    * <ul>
    * <li>last price in the dataset</li>
    * <li>last price in the selected range</li>
    * </ul>
    * @since 7.0.0
    */
  var currentPriceIndicator: js.Any = js.undefined

  /**
    * <p>Elliott wave (three points) annotation bindings. Includes <code>start</code> and two
    * events in <code>steps</code> (for second and third points) array.</p>
    * @since 7.0.0
    */
  var elliott3: js.Any = js.undefined

  /**
    * <p>Elliott wave (five points) annotation bindings. Includes <code>start</code> and four
    * event in <code>steps</code> (for all consequent points in Elliott wave) array.</p>
    * @since 7.0.0
    */
  var elliott5: js.Any = js.undefined

  var ellipseAnnotation: js.Any = js.undefined

  /**
    * <p>A fibonacci annotation bindings. Includes <code>start</code> and two events in
    * <code>steps</code> array (updates second point, then height).</p>
    * @since 7.0.0
    */
  var fibonacci: js.Any = js.undefined

  /**
    * <p>The Fibonacci Time Zones annotation bindings. Includes <code>start</code> and one
    * event in <code>steps</code> array.</p>
    * @since 7.0.0
    */
  var fibonacciTimeZones: js.Any = js.undefined

  /**
    * <p>A flag series bindings. Includes <code>start</code> event. On click, finds the
    * closest point and marks it with a flag with <code>&#39;circlepin&#39;</code> shape.</p>
    * @since 7.0.0
    */
  var flagCirclepin: js.Any = js.undefined

  /**
    * <p>A flag series bindings. Includes <code>start</code> event. On click, finds the
    * closest point and marks it with a flag with <code>&#39;diamondpin&#39;</code> shape.</p>
    * @since 7.0.0
    */
  var flagDiamondpin: js.Any = js.undefined

  /**
    * <p>A flag series bindings. Includes <code>start</code> event.
    * On click, finds the closest point and marks it with a flag without pin
    * shape.</p>
    * @since 7.0.0
    */
  var flagSimplepin: js.Any = js.undefined

  /**
    * <p>A flag series bindings. Includes <code>start</code> event.
    * On click, finds the closest point and marks it with a flag with
    * <code>&#39;squarepin&#39;</code> shape.</p>
    * @since 7.0.0
    */
  var flagSquarepin: js.Any = js.undefined

  /**
    * <p>Displays chart in fullscreen.</p>
    * <p><strong>Note</strong>: Fullscreen is not supported on iPhone due to iOS limitations.</p>
    * @since 7.0.0
    */
  var fullScreen: js.Any = js.undefined

  /**
    * <p>A horizontal line annotation. Includes <code>start</code> event.</p>
    * @since 7.0.0
    */
  var horizontalLine: js.Any = js.undefined

  /**
    * <p>Indicators bindings. Includes <code>init</code> event to show a popup.</p>
    * <p>Note: In order to show base series from the chart in the popup&#39;s
    * dropdown each series requires
    * <a href="https://api.highcharts.com/highstock/series.line.id">series.id</a> to be
    * defined.</p>
    * @since 7.0.0
    */
  var indicators: js.Any = js.undefined

  /**
    * <p>A line annotation. Includes <code>start</code> and one event in <code>steps</code> array.</p>
    * @since 7.0.0
    */
  var infinityLine: js.Any = js.undefined

  /**
    * <p>A label annotation bindings. Includes <code>start</code> event only.</p>
    * @since 7.0.0
    */
  var labelAnnotation: js.Any = js.undefined

  /**
    * <p>A measure (x-dimension) annotation bindings. Includes <code>start</code> and one
    * event in <code>steps</code> array.</p>
    * @since 7.0.0
    */
  var measureX: js.Any = js.undefined

  /**
    * <p>A measure (xy-dimension) annotation bindings. Includes <code>start</code> and one
    * event in <code>steps</code> array.</p>
    * @since 7.0.0
    */
  var measureXY: js.Any = js.undefined

  /**
    * <p>A measure (y-dimension) annotation bindings. Includes <code>start</code> and one
    * event in <code>steps</code> array.</p>
    * @since 7.0.0
    */
  var measureY: js.Any = js.undefined

  /**
    * <p>A parallel channel (tunnel) annotation bindings. Includes <code>start</code> and
    * two events in <code>steps</code> array (updates second point, then height).</p>
    * @since 7.0.0
    */
  var parallelChannel: js.Any = js.undefined

  /**
    * <p>An Andrew&#39;s pitchfork annotation bindings. Includes <code>start</code> and two
    * events in <code>steps</code> array (sets second and third control points).</p>
    * @since 7.0.0
    */
  var pitchfork: js.Any = js.undefined

  /**
    * <p>A ray annotation bindings. Includes <code>start</code> and one event in <code>steps</code>
    * array.</p>
    * @since 7.0.0
    */
  var ray: js.Any = js.undefined

  /**
    * <p>A rectangle annotation bindings. Includes <code>start</code> and one event
    * in <code>steps</code> array.</p>
    * @since 7.0.0
    */
  var rectangleAnnotation: js.Any = js.undefined

  /**
    * <p>Save a chart in localStorage under <code>highcharts-chart</code> key.
    * Stored items:</p>
    * <ul>
    * <li>annotations</li>
    * <li>indicators (with yAxes)</li>
    * <li>flags</li>
    * </ul>
    * @since 7.0.0
    */
  var saveChart: js.Any = js.undefined

  /**
    * <p>A segment annotation bindings. Includes <code>start</code> and one event in <code>steps</code>
    * array.</p>
    * @since 7.0.0
    */
  var segment: js.Any = js.undefined

  /**
    * <p>Changes main series to <code>&#39;candlestick&#39;</code> type.</p>
    * @since 7.0.0
    */
  var seriesTypeCandlestick: js.Any = js.undefined

  /**
    * <p>Change main series to <code>&#39;hlc&#39;</code> type.</p>
    * @since 7.0.0
    */
  var seriesTypeHLC: js.Any = js.undefined

  /**
    * <p>Changes main series to <code>&#39;heikinashi&#39;</code> type.</p>
    * @since 7.0.0
    */
  var seriesTypeHeikinAshi: js.Any = js.undefined

  /**
    * <p>Changes main series to <code>&#39;hollowcandlestick&#39;</code> type.</p>
    * @since 7.0.0
    */
  var seriesTypeHollowCandlestick: js.Any = js.undefined

  /**
    * <p>Changes main series to <code>&#39;line&#39;</code> type.</p>
    * @since 7.0.0
    */
  var seriesTypeLine: js.Any = js.undefined

  /**
    * <p>Changes main series to <code>&#39;ohlc&#39;</code> type.</p>
    * @since 7.0.0
    */
  var seriesTypeOhlc: js.Any = js.undefined

  /**
    * <p>A vertical arrow annotation bindings. Includes <code>start</code> event. On click,
    * finds the closest point and marks it with an arrow and a label with
    * value.</p>
    * @since 7.0.0
    */
  var timeCycles: js.Any = js.undefined

  /**
    * <p>Hides/shows all annotations on a chart.</p>
    * @since 7.0.0
    */
  var toggleAnnotations: js.Any = js.undefined

  /**
    * <p>A vertical arrow annotation bindings. Includes <code>start</code> event. On click,
    * finds the closest point and marks it with an arrow.
    * <code>#06b535</code> is the color of the arrow when
    * pointing from above and <code>#f21313</code>
    * when pointing from below the point.</p>
    * @since 7.0.0
    */
  var verticalArrow: js.Any = js.undefined

  /**
    * <p>A vertical counter annotation bindings. Includes <code>start</code> event. On click,
    * finds the closest point and marks it with a numeric annotation -
    * incrementing counter on each add.</p>
    * @since 7.0.0
    */
  var verticalCounter: js.Any = js.undefined

  /**
    * <p>A vertical line annotation. Includes <code>start</code> event.</p>
    * @since 7.0.0
    */
  var verticalLine: js.Any = js.undefined

  /**
    * <p>Enables zooming in xAxis on a chart. Includes <code>start</code> event which
    * changes <a href="#chart.zoomType">chart.zoomType</a>.</p>
    * @since 7.0.0
    */
  var zoomX: js.Any = js.undefined

  /**
    * <p>Enables zooming in xAxis and yAxis on a chart. Includes <code>start</code> event
    * which changes <a href="#chart.zoomType">chart.zoomType</a>.</p>
    * @since 7.0.0
    */
  var zoomXY: js.Any = js.undefined

  /**
    * <p>Enables zooming in yAxis on a chart. Includes <code>start</code> event which
    * changes <a href="#chart.zoomType">chart.zoomType</a>.</p>
    * @since 7.0.0
    */
  var zoomY: js.Any = js.undefined
}

object NavigationBindings {
  /**
    * @param arrowInfinityLine <p>A line with arrow annotation. Includes <code>start</code> and one event in <code>steps</code>. array.</p>
    * @param arrowRay <p>A ray with an arrow annotation bindings. Includes <code>start</code> and one event. in <code>steps</code> array.</p>
    * @param arrowSegment <p>A segment with an arrow annotation bindings. Includes <code>start</code> and one. event in <code>steps</code> array.</p>
    * @param circleAnnotation <p>A circle annotation bindings. Includes <code>start</code> and one event in. <code>steps</code> array.</p>
    * @param crooked3 <p>Crooked line (three points) annotation bindings. Includes <code>start</code> and two. events in <code>steps</code> (for second and third points in crooked line) array.</p>
    * @param crooked5 <p>Crooked line (five points) annotation bindings. Includes <code>start</code> and four. events in <code>steps</code> (for all consequent points in crooked line) array.</p>
    * @param currentPriceIndicator <p>Hides/shows two price indicators:</p>. <ul>. <li>last price in the dataset</li>. <li>last price in the selected range</li>. </ul>
    * @param elliott3 <p>Elliott wave (three points) annotation bindings. Includes <code>start</code> and two. events in <code>steps</code> (for second and third points) array.</p>
    * @param elliott5 <p>Elliott wave (five points) annotation bindings. Includes <code>start</code> and four. event in <code>steps</code> (for all consequent points in Elliott wave) array.</p>
    * @param fibonacci <p>A fibonacci annotation bindings. Includes <code>start</code> and two events in. <code>steps</code> array (updates second point, then height).</p>
    * @param fibonacciTimeZones <p>The Fibonacci Time Zones annotation bindings. Includes <code>start</code> and one. event in <code>steps</code> array.</p>
    * @param flagCirclepin <p>A flag series bindings. Includes <code>start</code> event. On click, finds the. closest point and marks it with a flag with <code>&#39;circlepin&#39;</code> shape.</p>
    * @param flagDiamondpin <p>A flag series bindings. Includes <code>start</code> event. On click, finds the. closest point and marks it with a flag with <code>&#39;diamondpin&#39;</code> shape.</p>
    * @param flagSimplepin <p>A flag series bindings. Includes <code>start</code> event.. On click, finds the closest point and marks it with a flag without pin. shape.</p>
    * @param flagSquarepin <p>A flag series bindings. Includes <code>start</code> event.. On click, finds the closest point and marks it with a flag with. <code>&#39;squarepin&#39;</code> shape.</p>
    * @param fullScreen <p>Displays chart in fullscreen.</p>. <p><strong>Note</strong>: Fullscreen is not supported on iPhone due to iOS limitations.</p>
    * @param horizontalLine <p>A horizontal line annotation. Includes <code>start</code> event.</p>
    * @param indicators <p>Indicators bindings. Includes <code>init</code> event to show a popup.</p>. <p>Note: In order to show base series from the chart in the popup&#39;s. dropdown each series requires. <a href="https://api.highcharts.com/highstock/series.line.id">series.id</a> to be. defined.</p>
    * @param infinityLine <p>A line annotation. Includes <code>start</code> and one event in <code>steps</code> array.</p>
    * @param labelAnnotation <p>A label annotation bindings. Includes <code>start</code> event only.</p>
    * @param measureX <p>A measure (x-dimension) annotation bindings. Includes <code>start</code> and one. event in <code>steps</code> array.</p>
    * @param measureXY <p>A measure (xy-dimension) annotation bindings. Includes <code>start</code> and one. event in <code>steps</code> array.</p>
    * @param measureY <p>A measure (y-dimension) annotation bindings. Includes <code>start</code> and one. event in <code>steps</code> array.</p>
    * @param parallelChannel <p>A parallel channel (tunnel) annotation bindings. Includes <code>start</code> and. two events in <code>steps</code> array (updates second point, then height).</p>
    * @param pitchfork <p>An Andrew&#39;s pitchfork annotation bindings. Includes <code>start</code> and two. events in <code>steps</code> array (sets second and third control points).</p>
    * @param ray <p>A ray annotation bindings. Includes <code>start</code> and one event in <code>steps</code>. array.</p>
    * @param rectangleAnnotation <p>A rectangle annotation bindings. Includes <code>start</code> and one event. in <code>steps</code> array.</p>
    * @param saveChart <p>Save a chart in localStorage under <code>highcharts-chart</code> key.. Stored items:</p>. <ul>. <li>annotations</li>. <li>indicators (with yAxes)</li>. <li>flags</li>. </ul>
    * @param segment <p>A segment annotation bindings. Includes <code>start</code> and one event in <code>steps</code>. array.</p>
    * @param seriesTypeCandlestick <p>Changes main series to <code>&#39;candlestick&#39;</code> type.</p>
    * @param seriesTypeHLC <p>Change main series to <code>&#39;hlc&#39;</code> type.</p>
    * @param seriesTypeHeikinAshi <p>Changes main series to <code>&#39;heikinashi&#39;</code> type.</p>
    * @param seriesTypeHollowCandlestick <p>Changes main series to <code>&#39;hollowcandlestick&#39;</code> type.</p>
    * @param seriesTypeLine <p>Changes main series to <code>&#39;line&#39;</code> type.</p>
    * @param seriesTypeOhlc <p>Changes main series to <code>&#39;ohlc&#39;</code> type.</p>
    * @param timeCycles <p>A vertical arrow annotation bindings. Includes <code>start</code> event. On click,. finds the closest point and marks it with an arrow and a label with. value.</p>
    * @param toggleAnnotations <p>Hides/shows all annotations on a chart.</p>
    * @param verticalArrow <p>A vertical arrow annotation bindings. Includes <code>start</code> event. On click,. finds the closest point and marks it with an arrow.. <code>#06b535</code> is the color of the arrow when. pointing from above and <code>#f21313</code>. when pointing from below the point.</p>
    * @param verticalCounter <p>A vertical counter annotation bindings. Includes <code>start</code> event. On click,. finds the closest point and marks it with a numeric annotation -. incrementing counter on each add.</p>
    * @param verticalLine <p>A vertical line annotation. Includes <code>start</code> event.</p>
    * @param zoomX <p>Enables zooming in xAxis on a chart. Includes <code>start</code> event which. changes <a href="#chart.zoomType">chart.zoomType</a>.</p>
    * @param zoomXY <p>Enables zooming in xAxis and yAxis on a chart. Includes <code>start</code> event. which changes <a href="#chart.zoomType">chart.zoomType</a>.</p>
    * @param zoomY <p>Enables zooming in yAxis on a chart. Includes <code>start</code> event which. changes <a href="#chart.zoomType">chart.zoomType</a>.</p>
    */
  def apply(arrowInfinityLine: js.UndefOr[js.Any] = js.undefined, arrowRay: js.UndefOr[js.Any] = js.undefined, arrowSegment: js.UndefOr[js.Any] = js.undefined, circleAnnotation: js.UndefOr[js.Any] = js.undefined, crooked3: js.UndefOr[js.Any] = js.undefined, crooked5: js.UndefOr[js.Any] = js.undefined, currentPriceIndicator: js.UndefOr[js.Any] = js.undefined, elliott3: js.UndefOr[js.Any] = js.undefined, elliott5: js.UndefOr[js.Any] = js.undefined, ellipseAnnotation: js.UndefOr[js.Any] = js.undefined, fibonacci: js.UndefOr[js.Any] = js.undefined, fibonacciTimeZones: js.UndefOr[js.Any] = js.undefined, flagCirclepin: js.UndefOr[js.Any] = js.undefined, flagDiamondpin: js.UndefOr[js.Any] = js.undefined, flagSimplepin: js.UndefOr[js.Any] = js.undefined, flagSquarepin: js.UndefOr[js.Any] = js.undefined, fullScreen: js.UndefOr[js.Any] = js.undefined, horizontalLine: js.UndefOr[js.Any] = js.undefined, indicators: js.UndefOr[js.Any] = js.undefined, infinityLine: js.UndefOr[js.Any] = js.undefined, labelAnnotation: js.UndefOr[js.Any] = js.undefined, measureX: js.UndefOr[js.Any] = js.undefined, measureXY: js.UndefOr[js.Any] = js.undefined, measureY: js.UndefOr[js.Any] = js.undefined, parallelChannel: js.UndefOr[js.Any] = js.undefined, pitchfork: js.UndefOr[js.Any] = js.undefined, ray: js.UndefOr[js.Any] = js.undefined, rectangleAnnotation: js.UndefOr[js.Any] = js.undefined, saveChart: js.UndefOr[js.Any] = js.undefined, segment: js.UndefOr[js.Any] = js.undefined, seriesTypeCandlestick: js.UndefOr[js.Any] = js.undefined, seriesTypeHLC: js.UndefOr[js.Any] = js.undefined, seriesTypeHeikinAshi: js.UndefOr[js.Any] = js.undefined, seriesTypeHollowCandlestick: js.UndefOr[js.Any] = js.undefined, seriesTypeLine: js.UndefOr[js.Any] = js.undefined, seriesTypeOhlc: js.UndefOr[js.Any] = js.undefined, timeCycles: js.UndefOr[js.Any] = js.undefined, toggleAnnotations: js.UndefOr[js.Any] = js.undefined, verticalArrow: js.UndefOr[js.Any] = js.undefined, verticalCounter: js.UndefOr[js.Any] = js.undefined, verticalLine: js.UndefOr[js.Any] = js.undefined, zoomX: js.UndefOr[js.Any] = js.undefined, zoomXY: js.UndefOr[js.Any] = js.undefined, zoomY: js.UndefOr[js.Any] = js.undefined): NavigationBindings = {
    com.highcharts.HighchartsGenericObject.toCleanObject(new NavigationBindings {
      this.arrowInfinityLine = arrowInfinityLine
      this.arrowRay = arrowRay
      this.arrowSegment = arrowSegment
      this.circleAnnotation = circleAnnotation
      this.crooked3 = crooked3
      this.crooked5 = crooked5
      this.currentPriceIndicator = currentPriceIndicator
      this.elliott3 = elliott3
      this.elliott5 = elliott5
      this.ellipseAnnotation = ellipseAnnotation
      this.fibonacci = fibonacci
      this.fibonacciTimeZones = fibonacciTimeZones
      this.flagCirclepin = flagCirclepin
      this.flagDiamondpin = flagDiamondpin
      this.flagSimplepin = flagSimplepin
      this.flagSquarepin = flagSquarepin
      this.fullScreen = fullScreen
      this.horizontalLine = horizontalLine
      this.indicators = indicators
      this.infinityLine = infinityLine
      this.labelAnnotation = labelAnnotation
      this.measureX = measureX
      this.measureXY = measureXY
      this.measureY = measureY
      this.parallelChannel = parallelChannel
      this.pitchfork = pitchfork
      this.ray = ray
      this.rectangleAnnotation = rectangleAnnotation
      this.saveChart = saveChart
      this.segment = segment
      this.seriesTypeCandlestick = seriesTypeCandlestick
      this.seriesTypeHLC = seriesTypeHLC
      this.seriesTypeHeikinAshi = seriesTypeHeikinAshi
      this.seriesTypeHollowCandlestick = seriesTypeHollowCandlestick
      this.seriesTypeLine = seriesTypeLine
      this.seriesTypeOhlc = seriesTypeOhlc
      this.timeCycles = timeCycles
      this.toggleAnnotations = toggleAnnotations
      this.verticalArrow = verticalArrow
      this.verticalCounter = verticalCounter
      this.verticalLine = verticalLine
      this.zoomX = zoomX
      this.zoomXY = zoomXY
      this.zoomY = zoomY
    })
  }
}
